*_erc.json
.claude/
*_drc_violations.json
~*.lck
_autosave-*
*-backups/

# Freerouting intermediate I/O — rebuilt by autoroute every run.
# The historical *_planes.dsn/.ses (kept for the layer-type experiment)
# are committed despite this pattern.
power_module.dsn
power_module.ses
